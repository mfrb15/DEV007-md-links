/* eslint-disable arrow-body-style */
import fs from 'fs';

export const mdLinks = (path, options) => {
  return new Promise((resolve, reject) => {
    // Identifica si la ruta existe.
    // Verifica si la ruta es absulota
    // Transforma la ruta a absoluta
    // Comprobar si la ruta es un directorio.
    // Si es un directorio filtrar los archivos
    // Si no exite la ruta rechaza la promera
    // console.log('error');
  });
};
